classe PongGame
var WIDTH = 800
var HEIGHT = 600
var FPS = 60

imprimir "Jogo iniciado"

met iniciar_objetos
    var BALL_SPEED = [5, 5]
    var PADDLE_SPEED = 8

met reiniciar_bola
met lidar_eventos
met lidar_entrada

met atualizar_bola
    var SPEED_INCREMENT = 0.3

met desenhar
    var COLOR = [150, 100, 105]
    var BACKGROUND = [0, 0, 0]

met executar